const hello = firstname => `Hello ${firstname} welcome into the wild`;
console.log(hello('Jonathan'));

const fruits = ['bananas', 'oranges', 'strawberries'];
const vegetables = ['carrots', 'louis'];

const concat = [...fruits, ...vegetables];

console.log(concat);
