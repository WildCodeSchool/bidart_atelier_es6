const hello = (firstName) => `Hello ${firstName} welcome into the wild`;


const fruits = ['ananas', 'abricot', 'coco'];
const vegetables = ['carrot', 'louis'];
const result = [...fruits, ...vegetables];
console.log(result)
